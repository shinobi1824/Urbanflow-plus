{
  "name": "urbanflow-plus",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "type-check": "tsc",
    "build": "tsc && vite build",
    "build:fast": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build:fast && npx cap sync",
    "android": "npm run build:fast && npx cap add android || true && npx cap sync android && npx cap run android",
    "android:full": "npm run build && npx cap add android || true && npx cap sync android && npx cap run android",
    "ios": "npm run build:fast && npx cap add ios || true && npx cap sync ios && npx cap run ios"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "mapbox-gl": "^3.1.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.4.5",
    "vite": "^5.4.11"
  }
}